package com.yoyo.launcher.compose

import androidx.annotation.Keep

/**
 * Stubbed ComposeFacade for standalone build.
 */
@Keep
object ComposeFacade {
    fun isComposeAvailable(): Boolean {
        return false
    }
}
