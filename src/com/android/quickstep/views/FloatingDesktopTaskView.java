package com.android.quickstep.views;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.FrameLayout;

public class FloatingDesktopTaskView extends FrameLayout {
    public FloatingDesktopTaskView(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
